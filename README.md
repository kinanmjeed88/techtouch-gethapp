# المساعد الشخصي الذكي - دليل المشروع

## 📝 نظرة عامة

هذا المشروع هو تطبيق ويب تفاعلي مصمم للعمل على الشاشات الصغيرة جدًا. يوفر واجهتين رئيسيتين: واجهة دردشة مع نموذج ذكاء اصطناعي (Gemini)، وواجهة لعرض معلومات شخصية محددة مسبقًا مع نظام بحث ذكي.

تم بناء التطبيق باستخدام React و TypeScript، مع استخدام Tailwind CSS للتصميم، ويتم بناؤه باستخدام Create React App (react-scripts).

---

## ✨ الميزات الرئيسية

- **دردشة ذكية:** تواصل مباشر مع Gemini API لفهم الصور والنصوص.
- **سلوك AI مخصص:** تم تزويد الذكاء الاصطناعي بمجموعة صارمة من القواعد لضمان تقديم إجابات موثوقة ومبنية على الحقائق.
- **قسم معلوماتي:** قاعدة بيانات محلية قابلة للبحث بسهولة عن معلومات وروابط محددة (قنوات تيليجرام، يوتيوب، إلخ).
- **بحث ذكي:** يفهم البحث عن المرادفات والكلمات المفتاحية المختلفة.
- **دعم الملفات:** إمكانية إرسال الصور للتحليل، وترجمة ملفات PDF و Word (DOCX).
- **تصميم متجاوب:** مصمم خصيصًا للشاشات الصغيرة جدًا.
- **عملية بناء ونشر قياسية:** يستخدم `npm run deploy` للنشر بسهولة على GitHub Pages.

---

## 📁 هيكل الملفات

```
.
├── README.md                 # ملف التعليمات هذا
├── package.json              # ملف إدارة الاعتماديات والأوامر
├── public/
│   └── index.html            # قالب HTML الرئيسي للتطبيق
└── src/
    ├── index.css             # ملف إعدادات Tailwind CSS
    ├── index.tsx             # نقطة دخول React، حيث يتم ربط التطبيق بالـ DOM
    ├── App.tsx               # المكون الرئيسي الذي يدير التنقل بين الواجهات
    ├── types.ts              # تعريفات TypeScript للأنواع المستخدمة
    ├── constants.ts          # يحتوي على الثوابت مثل تعليمات النظام والبيانات الشخصية
    ├── components/           # مجلد مكونات React القابلة لإعادة الاستخدام
    └── services/             # مجلد الخدمات للتواصل مع APIs ومعالجة البيانات
```

---

## 🚀 التشغيل والنشر

### لتشغيل المشروع محليًا

1.  تأكد من وجود Node.js و npm مثبتين على جهازك.
2.  قم بتثبيت الاعتماديات:
    ```bash
    npm install
    ```
3.  قم بتشغيل خادم التطوير المحلي:
    ```bash
    npm start
    ```
    سيفتح هذا الأمر المشروع في متصفحك على `http://localhost:3000`.

### لنشر المشروع على GitHub Pages

1.  تأكد من أنك قمت بإعداد مستودعك على GitHub.
2.  قم بتشغيل أمر النشر:
    ```bash
    npm run deploy
    ```
    سيقوم هذا الأمر تلقائيًا بـ:
    - بناء (build) نسخة الإنتاج من تطبيقك في مجلد `build`.
    - نشر محتويات مجلد `build` إلى فرع جديد اسمه `gh-pages` في مستودعك.

3.  **تغيير مصدر النشر في GitHub:**
    - اذهب إلى مستودعك (Repository) على موقع GitHub.
    - انقر على `Settings` (الإعدادات).
    - في القائمة الجانبية، انقر على `Pages` (الصفحات).
    - تحت قسم "Build and deployment"، ابحث عن `Source` (المصدر).
    - غيّر الفرع (Branch) من `main` (أو `master`) إلى `gh-pages`.
    - انقر `Save` (حفظ).

بعد لحظات قليلة، سيكون موقعك متاحًا على الرابط الذي يوفره GitHub Pages.

---

## 🔧 كيفية التعديل

### 1. لتغيير سلوك الذكاء الاصطناعي:

- اذهب إلى ملف `src/constants.ts`.
- قم بتعديل النص داخل المتغير `SYSTEM_PROMPT`.

### 2. لإضافة أو تعديل قنواتك وروابطك:

- اذهب إلى ملف `src/constants.ts`.
- ابحث عن المتغير `PERSONAL_DATA_STRUCTURED` وقم بتعديل المصفوفة.